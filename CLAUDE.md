# 项目规范

## Markdown 笔记标题编号规则

Obsidian 笔记的标题按层级使用不同编号格式：

- 一级标题（#）：中文序号，如 `一、` `二、` `三、`
- 二级标题（##）：阿拉伯数字+顿号，如 `1、` `2、` `3、`
- 三级标题（###）：括号数字，如 `（1）、` `（2）、` `（3）、`

示例：
```
# 一、概述
## 1、基本原理
### （1）、概念说明
```